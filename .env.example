# Copy this file to .env and fill in the two values. Never share or commit .env.
# API key: Passcreator > Integrations > API Keys. Ideally from a user who can only see the membership template.
PASSCREATOR_API_KEY=
# Template ID: use the TEST template first, then switch to the real membership template.
PASSCREATOR_TEMPLATE_ID=
